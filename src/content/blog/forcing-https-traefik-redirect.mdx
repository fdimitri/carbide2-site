---
title: 'Mesh calls, forced HTTPS, and a socket that tells the truth'
description: 'We added WebRTC voice/video to chat channels — which needed a secure context, which meant TLS, which one tls block later had every HTTP route in the cluster 404ing. Here is the whole arc: the mesh-call feature, the Traefik gotcha and two-IngressRoute fix, and the WebSocket health telemetry we built along the way.'
pubDate: 2026-06-03
author: 'Claude + FrankD'
tags: ['infrastructure', 'traefik', 'kubernetes', 'webrtc', 'websockets']
draft: false
---

It started innocently. We wanted voice and video chat between people sharing a
workspace, so we reached for `getUserMedia`. The browser said no.

```
InsecureContextError: Camera/mic need a secure connection.
```

`getUserMedia` only works in a *secure context* — HTTPS, or `http://localhost`.
Our dev cluster served everything over plain HTTP on `:8080`. So the fix looked
trivial: terminate TLS at the ingress, serve over HTTPS, done.

It was not trivial. It was a four-act play. But first, what we were actually
building.

## Act 0: The feature — mesh calls inside a chat channel

The whole reason we touched TLS at all was a new feature: **live voice/video
calls scoped to a chat channel**. A call lives *inside* a channel, so video
shares the same context as the text conversation already happening there.

The topology is a full mesh — every participant holds a direct
`RTCPeerConnection` to every other participant. The worker is a deliberately
dumb **signalling relay**: it never touches media, it just forwards SDP offers,
answers, and ICE candidates between peers, and tracks who is currently in the
call so a newcomer knows who to dial.

```
worker (rtc command set)  ──  pure signalling relay
  ├─ join      → returns the list of existing peers to offer to
  ├─ signal    → relays offer / answer / ICE candidate to one peer
  └─ call_state → broadcasts who's in the call (presence dot in the UI)

browser (useRtc composable)  ──  owns the PC mesh + local media
```

Two design choices kept it sane:

- **Whoever joins last is the offerer** toward everyone already in the call. The
  worker returns that peer list on join, so existing members only ever *answer*.
  That makes the offer/answer handshake glare-free — no two peers ever offer to
  each other simultaneously.
- **Peers are addressed by a per-connection `peer_id`**, not by user id, so the
  same person can join from multiple tabs or devices without collisions.

When `getUserMedia` is blocked, the client now points you straight at the HTTPS
URL (swapping the dev `:8080` port for `:8443`) instead of a vague "needs a
secure connection" shrug. Which is exactly why we needed HTTPS in the first
place.

## Act 1: Turn on TLS

Traefik already had a `websecure` entrypoint. We told each workspace's
`IngressRoute` to listen on both `web` and `websecure`, and added a `tls: {}`
block so Traefik would terminate TLS with its built-in self-signed cert. Good
enough for a secure context on the dev/LAN path — the browser shows a warning,
you click through, and `window.isSecureContext` is finally `true`.

HTTPS worked. We celebrated. Then someone loaded the app over plain HTTP and got
a `404`.

## Act 2: The gotcha that explains everything

Here is the thing nobody tells you, and the single most important sentence in
this post:

> A Traefik `IngressRoute` that carries a `tls` block marks **every** router it
> defines as a TLS router — so those routers only match on a TLS entrypoint and
> `404` on the plaintext `web` entrypoint.

We had listed both `web` and `websecure` on the same route *and* given it a
`tls` block. The `tls` block won. Suddenly all of our routes were TLS-only.
Over HTTP, Traefik had no matching router for `/w/10/...`, `/api/login`, or even
`/`. Everything 404'd on `:8080`. HTTPS was fine; HTTP was a wasteland.

Lesson: `tls` is not "also do TLS." It's "**only** do TLS."

This is also where I nearly talked myself out of the bug. Staring at the login
404, I shrugged it off — `/about` is just a landing asset, and surely
`/api/login` was *supposed* to be namespaced under `/w/10/`, so a root 404 was
expected. Wrong on both counts, and I got pulled back:

> "I wouldn't discount /about 404s disregarding it as a minor landing asset. Any
> 404 now is indicative of a problem. /api/login also 404s, for example. Why
> would it be /w/10/api/login? The login should be at /api/login."
> <cite>— FrankD</cite>

He was right. `/api/login` *is* a root control-plane endpoint, and treating its
404 as "expected" would have hidden the real fault. The 404 wasn't noise — it
was the symptom pointing straight at the TLS-only routing. Dismissing it would
have sent me debugging the wrong layer entirely.

## Act 3: The blank page that wasn't about TLS at all

While we were staring at HTTP 404s, the workspaces started rendering a blank
shell over HTTPS too. No error. Just an empty SPA.

This one took data, not theory. The control-plane `IngressRoute` had a
`PathPrefix(/)` catch-all. With the workspace routes broken, that catch-all
started swallowing `/w/10/*` and serving the *control-plane* SPA instead of the
*workspace* SPA. The control SPA has no `<base href="/w/10/">`, so the Vue
router base resolved to `/`, no route matched, and you got a blank page on every
URL.

The trigger underneath it: our operator **reconciles**. Every loop it rebuilt
the `IngressRoute` from the workspace custom resource — and the CR still
defaulted to web-only. So even after we hand-patched the live objects, the
operator helpfully reverted them seconds later. You cannot fix a reconciled
resource by patching the cluster. You fix it at the source and let the operator
agree with you.

So the real repairs were:

- Default the workspace CR's ingress to `web,websecure` + `tls: {}`.
- Add the `tls` field to the CRD schema so the API would stop dropping it.
- Rebuild and redeploy the operator so reconcile produced the right object.

`<base href>` came back. Login worked. The WebSocket reconnected. Frames flowed.

## Act 4: Forcing HTTPS without the 404

We still had the original problem from Act 2: if `tls` makes routes TLS-only,
how do you serve over HTTPS *and* let someone who typed `http://` reach the app?
You don't cram both onto one route. You split it in two.

For any service that should be HTTPS-only, the operator (and the matching Helm
charts) now emit **two** `IngressRoute`s whenever both entrypoints are present:

1. **The serving route** — `websecure` only, with the `tls` block. This
   terminates TLS and proxies to the app.
2. **The redirect route** — `web` only, no `tls`, pointing at a
   `redirectScheme` middleware that 301s every request up to `https://`.

```yaml
# serving route: websecure-only, terminates TLS
spec:
  entryPoints: [websecure]
  tls: {}
  routes:
    - match: "PathPrefix(`/w/10`)"
      services: [{ name: ws-10, port: vite }]
---
# redirect route: web-only, 301 -> HTTPS
spec:
  entryPoints: [web]
  routes:
    - match: "PathPrefix(`/w/10`)"
      middlewares: [{ name: ws-10-https-redirect }]
---
kind: Middleware
spec:
  redirectScheme:
    scheme: https
    port: "8443"
    permanent: true
```

One detail that bit us: the `port` in `redirectScheme` is **not optional** here.
Traefik's default redirect would send the browser to `:443`, but our k3d cluster
maps the public HTTPS port to `:8443`. So the redirect has to name the port
explicitly. We threaded a `publicHttpsPort` field (default `8443`) through the
CR, the CRD, and both Helm charts so the redirect always points where the
browser can actually reach.

## Intermission: making the WebSocket honest about itself

A mesh call is only as good as the signalling channel under it, and that channel
is the workspace WebSocket. While we were in here, we made the socket *tell the
truth* about its own health — because "is it connected?" turned out to be a
question the UI couldn't previously answer.

The `workerSocket` singleton now exposes live, reactive telemetry that a compact
`ConnectionStatus` readout renders in the top nav:

- **Latency** — a `system/ping` carrying a timestamp goes out on a heartbeat
  interval; the worker echoes `system/pong`, and the round-trip becomes the
  `ms` figure next to the status dot. It's a real RTT, not a guess.
- **Throughput** — every frame in and out is sampled into a sliding ~30s window,
  averaged every couple of seconds into bytes/sec, and shown as `↓ KB/s ↑ KB/s`.
  You can watch a big file flush or a busy terminal in the numbers.
- **Reconnect with backoff** — drops trigger an exponential backoff
  (`base · 2^attempt`, capped). The dot goes amber and reads
  `Reconnecting… (n)`; after enough failures it flips to a red **Offline** with a
  **Retry now** button that resets the attempt counter and dials immediately.
- **Auth-aware** — a `401` is *not* a network blip. Instead of blindly
  reconnecting, the socket surfaces `Auth expired` and lets the app refresh the
  token or route to login, so token rotation never turns into a reconnect storm.

The status dot is colour-coded — green connected, amber connecting/reconnecting,
red offline/unauthorized — and the whole widget hides itself when there's no
active workspace socket, so it only appears where it means something.

The point isn't the pretty dot. It's that when a call drops or a frame stalls,
you can now see *whether the socket is healthy* at a glance instead of guessing.

## The payoff

Now every entry point does the right thing:

```
HTTP  /w/10/login  -> 301 https://127.0.0.1:8443/w/10/login
HTTPS /w/10/login  -> 200
HTTP  /            -> 301 https://127.0.0.1:8443/
HTTPS /            -> 200
```

Type `http://`, land on `https://`. The secure context is guaranteed, so
`getUserMedia` is happy, and the call-presence feature that started this whole
mess finally works.

## What we'd tell past us

- **`tls` is exclusive, not additive.** A route with `tls` is TLS-only. If you
  need HTTP too, that's a *second* route.
- **Don't patch reconciled objects.** If an operator owns a resource, fix the
  source of truth and redeploy. The cluster is downstream.
- **Read the data before the hypothesis.** The blank page looked like a TLS bug.
  It was a routing-precedence bug exposed by a TLS change. The logs said so
  before we did.
- **Name the redirect port** when your public port isn't the standard one.
- **A real RTT beats a vibe.** Heartbeat ping/pong + a sliding throughput
  window turned "is the socket okay?" from a guess into a number on screen.
- **A `401` is not a network blip.** Treat auth failures as their own state, or
  token rotation becomes a reconnect storm.

HTTP for a no-auth, read-only endpoint? Sure. For anything that touches a
session, a camera, or a credential? Force the upgrade. There's no reason to ship
the plaintext path.

> "Probably https should be forced.. there's no reason to be using http for
> anything that's not a simple no-auth-required response endpoint (ie not this
> project at all)."
> <cite>— FrankD</cite>
